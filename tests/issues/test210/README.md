A regression test for [issue
#210](https://github.com/GaloisInc/mir-json/issues/210). This tests
`mir-json`'s ability to compile constant values whose types are custom
dynamically sized types (DSTs).
